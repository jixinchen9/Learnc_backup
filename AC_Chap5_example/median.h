#ifndef GUARD_median_h
#define GUARD_median_h

//header file for median finding
#include <vector>
#include <list>

double median(std::vector<double>);

double median_analysis(const std::list<Student_info>&);
#endif