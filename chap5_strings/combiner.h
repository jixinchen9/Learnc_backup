#ifndef GUARD_combiner
#define GUARD_combiner
#include <string>
#include <vector>

std::vector<std::string> vconcat(std::vector<std::string>&, std::vector<std::string>&);

std::vector<std::string> hconcat(std::vector<std::string>&, std::vector<std::string>&);

#endif // !1
