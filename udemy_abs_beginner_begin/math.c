#include "math.h"

int add_numbers(int a, int b) {
	return a + b;
}

signed int substr_numbers(int a, int b) {
	return a - b;
}

float dvd_numbers(int a , int b) {
	return (float)a / b;
	//you can type cast a variable to influence the type of a result
}