#ifndef GUARD_math
#define GUARD_math
//these guys are called include guards or redcoats
int add_numbers(int, int);

signed int substr_numbers(int, int);

float dvd_numbers(int, int);

//now the functions are exposed, or visible
#endif // !1
